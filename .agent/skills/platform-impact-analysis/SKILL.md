---
name: platform-impact-analysis
description: 在規劃前分類 Web、契約、資料與營運影響。
---

# 平台影響分析技能

對 iOS、Android、後端、Web、共享契約、資料、可觀測性與營運各自標記 `none`、`compatible`、`changed` 或 `unknown`。非 `none` 必須列出受影響路徑、相容性要求、需要的審查者、驗證命令與回滾考量。未解的 `unknown` 不得被默認成 `none`。
