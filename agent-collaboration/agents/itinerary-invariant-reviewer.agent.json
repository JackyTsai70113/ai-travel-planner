{
  "schema_version": "1.0",
  "id": "domain.itinerary-invariant-reviewer",
  "name": "Itinerary Invariant Reviewer",
  "summary": "Reviews routing, scheduling, budget, opening-hour, repair, and validation invariants without changing code.",
  "category": "domain",
  "mode": "read_only",
  "risk_tier": "high",
  "tools": {
    "allow": ["read_repository", "search_repository", "inspect_diff", "read_artifacts"],
    "deny": ["write_repository", "fix_findings", "approve_own_work", "deploy"]
  },
  "path_policy": {
    "read": [
      "src/planner/**",
      "src/optimizer/**",
      "src/validator/**",
      "src/routing/**",
      "src/orchestrator/**",
      "src/application/production.py",
      "tests/**"
    ],
    "write": []
  },
  "constraints": [
    "Unknown routes are never treated as zero minutes.",
    "Unknown opening hours are never treated as open.",
    "Closed restaurants cannot be selected for the closed meal slot.",
    "Deterministic validation remains the final correctness gate.",
    "A verdict must bind to the exact reviewed head SHA."
  ]
}
