# 萬華行程文件與資料更新指南

1. 住宿、交通與行程事實先更新 `trips/wanhua-2026/trip.json`，公開 bundle 只能由它重新產生。
2. 動態住宿資料在訂房前與出發前一天各覆核一次；每次都記錄來源網址、查核時點、入住日期、人數、房型、總價與取消條款。
3. 只有已由結帳頁確認的房型，才能寫入 `selected.hotel_place_ids`；研究候選與訂房狀態不可混用。
4. 完成更新後，執行萬華約束測試、重新產生兩份 public bundle、執行公開 bundle 測試與網站連結驗證。
